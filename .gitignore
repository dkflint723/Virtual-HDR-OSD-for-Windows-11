.venv
.uv
.python
__pycache__
src/sdr_hdr_profile_creator/__pycache__
.build-venv
build-portable

# Portable EXE build output. Regenerated by
# "3- (Advanced users & developers) - Build Portable EXE.bat"; distributed as a
# GitHub release asset rather than tracked here, so the binary can never drift
# out of sync with the source committed beside it.
release/