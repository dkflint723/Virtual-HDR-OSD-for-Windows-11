"""Virtual HDR OSD for Windows."""

# The one place the version is written. pyproject.toml carries the same string, a test
# holds the two together, and everything that reports a version reads it from here.
__version__ = "1.1.0"
